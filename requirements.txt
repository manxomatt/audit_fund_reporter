# Core (required to run Phases 2-5 offline)
pdfplumber>=0.11,<0.12
networkx>=3.4,<4
openpyxl>=3.1,<4
PyYAML>=6,<7

# Optional: only needed for `--llm` narrative (constraint 3 firewall still applies)
anthropic>=0.40 ; python_version >= "3.9"
